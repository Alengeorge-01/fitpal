# Postgres
POSTGRES_USER=fitpal
POSTGRES_PASSWORD=change_me # pragma: allowlist secret
POSTGRES_DB=fitpal_dev
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# Mailpit
MAILPIT_PORT=8025
MAILPIT_SMTP_PORT=1025

# Django
DJANGO_SECRET_KEY=change_me # pragma: allowlist secret
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Next.js
NEXT_PUBLIC_APP_NAME=FitPal
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Supabase (wire later)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_JWT_SECRET= # pragma: allowlist secret
